import React from 'react';
// import { useQuery } from '@apollo/client';
// import { QUERY_JOB_POSTS } from '../utils/queries';

const someStyle = {
	//add styles
};

const Jobs = () => {
	// const { loading, data } = useQuery(QUERY_JOB_POSTS);
	// const jobPosts = data?.jobPosts || [];

	return <div style={someStyle}></div>;
};

export default Jobs;
