// Card for posting jobs. Must include the post job form for posting a new job, one that follows the correct schema: 

import React from "react";
// import "bootstrap/dist/css/bootstrap.css";

const postJobCard = {
 
};


function PostJob() {
 return (
  <>
   <card style={postJobCard}>
      <div>
        <>POST JOB HERE FORM</>
      </div>
   </card>
  </>
 );
}

export default PostJob;
